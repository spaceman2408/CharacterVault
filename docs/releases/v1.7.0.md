# CharacterVault 1.7.0

This release makes the editor **AI toolbar customizable** (reorder, hide, and create your own buttons), adds **roleplay highlight colors** for dialogue, narration, and actions, and ships a batch of editor quality-of-life fixes.

**Try it:** https://vault.charactervault.app  
**Staging:** https://staging.charactervault.app  
**Docs:** https://vault.charactervault.app/docs/

---

## Highlights

### Customizable AI toolbar

The fixed toolbar at the top of every editor is now yours to arrange. Open **Settings** in the workspace header, then the **Prompts** tab — the **Toolbar Buttons** card at the top controls which buttons appear and in what order. Remember to **Save Settings** when done.

- **Reorder** — use the ↑ ↓ arrows on a button to move it left or right.
- **Remove** — use the trash can to take a built-in button off the toolbar. Removed built-ins wait under **Add buttons** and can be put back at any time.
- **Bulk remove** — click **Select**, tick several buttons (or click their labels), then **Remove** / **Delete** once. One confirmation covers the whole batch.
- **Reset** — **Reset toolbar to defaults** restores the original set and order. Asks for confirmation first.
- **Custom can't be removed** — the 💬 **Custom** button is always kept (it shows a lock instead of a trash can), though you can move it.

Removing a button asks for confirmation: deleting a custom button is permanent, while removing a built-in just hides it.

Buttons run left to right in your order; when space runs out, extras collapse into **▼ More** automatically. Labels longer than 12 characters are shortened (hover a button for its full name).

[Text Editor → AI toolbar](/features/editor#ai-toolbar) · [AI Setup → Prompts](/configuration/ai-setup#prompts-tab)

### Custom buttons

Under **Add buttons** you can also create your own buttons via **New custom button**:

1. Pick a **label** (up to 40 characters), an **icon**, and a **color**.
2. Write the **prompt template** — it must contain `${text}`, which is replaced with your selection when the button runs.
3. Click **Add**. The button lands at the end of the toolbar and its editor opens automatically.

Each custom button can use its own AI endpoint and model: expand its editor (**Edit** or click its label) and set **Model for this prompt**, the same picker the built-in prompts use. Edit the label, icon, color, or template any time the same way; delete it with the trash can (with confirmation).

Custom buttons behave like the polish operations: they need a text selection, and their results preview as ghost text with Accept / Reject exactly like the built-ins.

[Text Editor → Custom buttons](/features/editor#custom-buttons)

### Roleplay highlight colors

Prose editors now color-code roleplay text so dialogue, narration, and actions are easy to tell apart. Coloring is on by default in every prose editor.

- **Dialogue** — text in `"double quotes"` (straight or curly). Defaults to amber.
- **Narration** — plain prose outside quotes and asterisks. Follows the editor text color unless you pick one.
- **Actions** — text in single `*asterisks*`. Defaults to gray. `**bold**` markers are never treated as actions, and unclosed quotes stay narration.

Change the colors under **Settings → Character Workspace → Roleplay colors**: each row has a color picker and a hex field, plus a live preview and a reset to defaults. `{{char}}` and `{{user}}` always keep their macro colors, even inside quotes.

`{{char}}` / `{{user}}` follow the theme by default. To override one, open **Settings → Character Workspace → Name macro colors**, pick a color with the picker or type a hex value. Clear the field (back to `auto`) to return to automatic.

[Text Editor → Roleplay colors](/features/editor#roleplay-colors)

### Editor quality-of-life

- **Current line** highlights only when nothing is selected, so selections read cleanly.
- **Search** (`Ctrl+F`) opens with the current selection prefilled; `Ctrl+H` jumps straight to Replace.
- **Font size** (`aA`) — the slider works with arrow keys, plus a Reset button and `Ctrl+=` / `Ctrl+-` / `Ctrl+0` shortcuts.
- **Macro expansion** — typing `Char` or `USER` plus punctuation expands to `{{char}}` / `{{user}}` (case-insensitive).
- **Creator Notes split preview** keeps undo history and focus when toggling preview.
- **Empty editors** show a placeholder hint; touch devices no longer pop the keyboard on section switch.

[Text Editor](/features/editor)

### Also in this release

- The AI payload preview button joins the utilities group while staying reachable in Custom mode; previews rebuild debounced under a content-hashed session key.
- Toolbar layout and highlight colors persist in settings, and are included in settings backup and full vault backup restore.
- Toolbar settings edits apply as functional draft updates, so rapid reorder/remove clicks can't overwrite each other.

---

## Fixes

- Editor search focus is tracked per-editor with guarded timeouts, so switching sections can't steal focus.
- The payload preview copy timer is cleaned up on close instead of lingering.
- Settings backup download revokes its object URL after saving instead of leaking it.

---

## Upgrade

Client-only app. Open the hosted site or rebuild locally; no server migration.

```bash
git clone https://github.com/spaceman2408/CharacterVault
cd CharacterVault
npm install
npm run dev
```

Local data from 1.6.0 keeps working. Toolbar layout, highlight colors, and per-button models save on the existing settings record. Hard refresh if the UI looks stale after deploy.

---

## Docs

- [Text Editor](/features/editor)
- [AI Setup](/configuration/ai-setup)
- [What's New](/whats-new)
- [Changelog](/changelog)
