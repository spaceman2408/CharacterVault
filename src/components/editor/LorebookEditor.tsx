/**
 * Re-export shared lorebook editor.
 * Implementation lives in ./lorebook/ (split modules).
 */
// eslint-disable-next-line react-refresh/only-export-components -- thin re-export barrel
export { LorebookEditor, default } from './lorebook/LorebookEditor';
